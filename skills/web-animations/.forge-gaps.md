# Forge Gaps — /web-animations

Reading: `.forge-spec.md` (success criteria) + `.forge-score.md` (deductions) + `.forge-sources.md` (external patterns).
Output: per-gap action list for Phase 5 rebuild.

---

## A. Score deductions → rebuild actions

| Deduction (Phase 2) | Action in rebuild |
|---|---|
| Site-type matrix covers only 4 types (need ≥10) | Add `references/site-types.md` with 12 classifications; SKILL.md top section becomes "Site Diagnosis" with each type's motion ceiling |
| Tier 3 gating absent (Local Service, Docs, Dashboard, Content/Pub allowed Tier 3) | New "Tier-by-Site-Type" matrix table in SKILL.md body — each type gets explicit `MAX_TIER` value. References file expands per type |
| SKILL.md 815 > 600 cap | Strip inline code samples (kit is source). Move Tier 1/2/3 code reference to `kit/README.md`. Move debugging block and a11y deep-dive to `references/`. Target ≤500 lines |
| Inline snippets duplicate kit components | Delete every code fence in SKILL.md body except short orchestration examples (≤8 lines per fence, only where it differs from kit) |
| No per-route role classification | Add "Route Role Matrix" — homepage / contact / pricing / blog index / article / dashboard / checkout / docs etc. Each role gets a different motion ceiling |
| Bundle gzip sizes not traceable | Cite bundlephobia URLs inline next to each library size. Move full table to `references/bundle-budget.md` |
| SKILL.md banned-phrase verifiability | After rebuild, grep SKILL.md for the banned list. Verify zero hits. Self-report in artifact summary |
| Touch-only + hover:hover only handled for one component | Document the kit-wide convention: every Tier 2/3 mouse-driven component guards with `(hover: hover)`. Add to grader logic check |
| Reduced-motion Playwright spec is template (requires dev server) | Acknowledge as "kit consumer's responsibility" — keep template, document the contract in `kit/README.md` |

---

## B. External-source patterns → rebuild applications

| Source | Pattern | Currently in skill? | Action |
|---|---|---|---|
| 1 — Motion.dev `useReducedMotion` | Reactive boolean; substitute, don't strip | YES (NumberTicker, MagneticButton, CursorParallax, TiltCard, CardStack) | Codify as kit-wide rule in `references/reduced-motion.md` |
| 2 — GSAP `matchMedia` dual-branch | Two named branches per preference, both code paths live | PARTIAL (PinnedSection uses single-branch early-return) | Refactor PinnedSection to use `gsap.matchMedia()` dual-branch — add reduce path that runs static layout, not just early return |
| 3 — WCAG 2.3.3 | Floor: every interaction-triggered animation MUST be disableable | PARTIAL (grader checks for reducedMotionRe but does not verify it's tied to the actual animation) | Grader v2: detect every motion call site, verify a guard is in the same component scope, not just file-level |
| 4 — WebKit six vestibular triggers | Six categories that fail floor regardless of tier | NO | Add `references/vestibular-triggers.md` enumerating the 6; grader v2 flags `useScroll`, `parallax`, `rotate`, `scale > 1.5`, `blur`, peripheral marquees without guards |
| 5 — Smashing classification table | Three buckets: triggering / safe / substitution-strategy | NO | Add `references/animation-classification.md` — table maps each Tier component to bucket. Grader v2 reads this when assessing kit code |
| 6 — MDN substitute-not-strip | `pulse → dissolve` not `pulse → none` | YES in kit, NOT documented in SKILL.md | Document the rule in SKILL.md "Floor rules" section, link to references file |
| 7 — Chrome scroll-driven CSS | Zero-JS Tier 3 for content/blog/docs sites | NO (kit has no CSS-timeline component) | Add `kit/src/tier3/CSSScrollReveal.tsx` + `kit/src/tier3/CSSScrollProgress.tsx` (sets CSS custom properties + injects keyframes); falls back to opacity if `animation-timeline` unsupported |
| 8 — View Transitions API | Zero-library route transitions | NO (kit has no ViewTransition wrapper) | Add `kit/src/tier3/ViewTransition.tsx` + helper `navigateWithTransition()` for any router. Bypass on `e.hasUAVisualTransition` |

---

## C. New artifacts produced by rebuild

| File | Purpose |
|---|---|
| `references/site-types.md` | 12 site types × motion ceiling × characteristic routes × banned moves |
| `references/route-roles.md` | 10+ route roles × motion ceiling per route × component allowlist |
| `references/floor-rules.md` | WCAG 2.3.3, 6 vestibular triggers, substitute-not-strip — consolidated |
| `references/animation-classification.md` | Smashing table + which kit components belong to which bucket |
| `references/bundle-budget.md` | Library × gzipped size × source URL × Tier-required-from |
| `references/debugging.md` | The 10 failure modes (moved out of SKILL.md body) |
| `references/a11y.md` | A11y deep-dive (moved out of SKILL.md body) |
| `kit/src/tier3/CSSScrollReveal.tsx` | Zero-JS reveal — `animation-timeline: view()` |
| `kit/src/tier3/CSSScrollProgress.tsx` | Zero-JS progress bar — `animation-timeline: scroll()` |
| `kit/src/tier3/ViewTransition.tsx` | `document.startViewTransition` wrapper |

---

## D. Banned-phrase strip (SKILL.md)

Search list before save: `comprehensive, robust, production-ready, world-class, premium, perfect, 10/10, shit hot, epic, best-in-class, enterprise-grade, battle-tested, deeply, holistic, seamless, cutting-edge`. Grep the new SKILL.md. Any hit → rewrite that section. Pairs with [[feedback_no_self_quality_claims]].

---

## E. Length contract

- SKILL.md ≤ 500 lines
- Each `references/*.md` ≤ 300 lines
- `kit/README.md` ≤ 200 lines

Current SKILL.md is 815. Stripping inline code + moving 4 sections to references should land it ≤ 420.
