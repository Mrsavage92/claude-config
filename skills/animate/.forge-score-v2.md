```json
{
  "total": 87,
  "dimensions": {
    "output_existence": {
      "score": 10,
      "max": 10,
      "evidence": "Artifact at C:/Users/Adam/audit-genius/.review/animate-artifact-v2.md contains 4 concrete proposals (Proposal 1 through Proposal 4), each with explicit Before/After code blocks for named files. Proposals 1 and 4 target Pricing.tsx; Proposals 2 and 3 target PricingCards.tsx. All are structured file-level diffs.",
      "deduction_reason": null
    },
    "spec_alignment": {
      "score": 14,
      "max": 20,
      "evidence": [
        "SC1 (Concrete edits, not advice): PASS — 4 proposals each contain full Before/After code blocks with specific file paths and line references. No bullet-list-only prose recommendations.",
        "SC2 (Respects project tier ceiling): PASS — site-profile.json confirmed /pricing motion_ceiling=1. Artifact reads the ceiling and stays at Tier 1 throughout. Proposal 2 explicitly REMOVES a Tier 2 violation (layoutId). No Tier 2+ proposals made.",
        "SC3 (Reduced-motion guard on every Tier 2+ proposal): PARTIAL PASS — Proposals 1, 3, and 4 include explicit useReducedMotion() guards. Proposal 2 removes a Tier 2 element and replaces with CSS transition; artifact argues the 150ms background-color on a small surface is not a vestibular trigger but does not add @media (prefers-reduced-motion: reduce) to null out the CSS transition. This is defensible but not fully conformant with Floor 2 (substitute, not strip — the CSS transition is the substitute, but no explicit @media block is shown). -2 for the gap.",
        "SC4 (Uses motion/react, not framer-motion): PASS — Proposal 1 imports: 'import { motion, useReducedMotion } from \"motion/react\"'. Verified against Pricing.tsx which has no motion import yet. PricingCards.tsx line 5 already uses 'from \"motion/react\"' — artifact correctly inherits this.",
        "SC5 (One hero moment, not maximalism): PASS — Artifact explicitly designates Proposal 1 (eyebrow → h1 → sub-copy stagger) as the single hero moment. Section 'What I deliberately did NOT propose' lists 6 rejected proposals with rationale. Total animated elements across 4 proposals: 3 heading elements (P1) + 1 button (P4). Well under the 6-element cap.",
        "SC6 (No banned phrases): PASS — No banned phrases found in artifact. See banned_phrase_hits section.",
        "SC7 (Performance budget acknowledged): PASS — Every proposal cites compositor-only properties. Proposal 1: 'opacity and y (translateY — transform)'. Proposal 2: removes motion.span, no new animated properties. Proposal 4: 'scale: 0.97 animates transform only'. Explicit Floor 5 citations on compositor-only throughout.",
        "SC8 (A11y beyond reduced-motion): PASS — Proposal 2 adds aria-pressed to billing toggle buttons. Proposal 4 adds aria-expanded + aria-controls + id to comparison table pattern. Proposal 1 explicitly notes 'No ARIA replacement needed — this motion communicated no state.' All per aria-state-replacements.md."
      ],
      "deduction_reason": "SC3 partial: Proposal 2 replaces a Tier 2 motion with CSS transition-colors but does not show a @media (prefers-reduced-motion: reduce) { .pricing-toggle { transition: none; } } block. Artifact acknowledges this gap ('can be added to the global stylesheet if the audit grader flags it') but does not include the code. -2 for incomplete guard on the replacement pattern."
    },
    "external_pattern_alignment": {
      "score": 20,
      "max": 20,
      "evidence": [
        "Source 1 (useReducedMotion hook, motion.dev): APPLIED — Proposals 1, 3, and 4 all call useReducedMotion() at the component root and branch animate/initial prop values inline on the same render path. Proposal 1: 'initial={prefersReduced ? false : { opacity: 0, y: 6 }}'. This matches the Source 1 pattern exactly ('Call useReducedMotion() at the component root to get a boolean, then branch animate prop values inline').",
        "Source 2 (Variants & Entrance Animations, motion.dev): APPLIED — Proposal 1 uses staggered entrance with stagger via delay offsets (0, 0.07, 0.13). The existing stagger grid in PricingCards.tsx (which Proposal 3 preserves) uses variants object with staggerChildren: 0.08 — the artifact analyzes this and marks it compliant rather than replacing it, which demonstrates pattern recognition. The artifact does not introduce a variants object in Proposal 1 (uses inline props with delay offsets instead) — partially applied. Award 4 pts.",
        "Source 3 (Gesture Micro-interactions, motion.dev): APPLIED — Proposal 4 uses whileTap={prefersReduced ? undefined : { scale: 0.97 }} on a motion.button, matching 'Attach whileHover and whileTap directly to interactive elements with small scale transforms'. No onMouseEnter/useState pattern used.",
        "Source 4 (Layout Animations — anti-pattern avoidance): APPLIED INVERSELY — Artifact does NOT propose new layoutId usage; it explicitly removes the existing layoutId='pricing-switch' (Proposal 2) on grounds that it is Tier 2 / banned for the pricing role. The spec constraint overrides Source 4's positive recommendation. Artifact correctly cites 'layoutId is explicitly listed in Banned moves.' This is appropriate prioritization of site-ceiling constraint over the layout-animation pattern. Count as applied (negative application = correct avoidance).",
        "Source 5 (BlurFade reusable wrapper, MagicUI): NOT APPLIED — No parameterised blur-fade component proposed. Artifact stays with inline motion props per all proposals. The per-role recipe for Tier 1 (pricing) does not require blur-fade, so absence is consistent with ceiling compliance, but the pattern was not used.",
        "Source 6 (Production Pricing Page stagger, GitHub): APPLIED — Artifact analyzes the existing PricingCards.tsx stagger grid (staggerChildren: 0.08, itemVariants hidden: { opacity: 0, y: 18, scale: 0.985 } → show: { opacity: 1, y: 0, scale: 1 }) and marks it compliant. This is calibrated to the Source 6 pattern: 'staggerChildren: 0.1 drives itemVariants (opacity: 0, y: 20 → opacity: 1, y: 0)'. Existing whileHover y: -6 is within the 'scale 1.02 + shadow is the floor, scale 1.1 is the ceiling' calibration range.",
        "Source 7 (Smashing Magazine — opt-in motion): NOT FULLY APPLIED — Artifact uses JS-side useReducedMotion() throughout but does not demonstrate CSS opt-in @media (prefers-reduced-motion: no-preference) blocks. CSS transition in Proposal 2 is written unconditionally rather than inside a no-preference block. Partially aligned.",
        "Source 8 (Smashing Magazine — swap property keep timing): APPLIED — Proposal 2's reduced-motion path for the billing toggle: CSS color transition at 150ms survives; no spatial motion. Proposal 1 collapses initial to false under reduced motion (instant opacity) — the timing/easing values are kept on the full-motion path. Pattern of 'swap property, keep timing' is followed.",
        "Source 9 (Josh Comeau asymmetric timing): NOT APPLIED — None of the proposals use asymmetric hover-in/out timing (e.g. 125ms in, 450ms out). Proposal 4 uses a single whileTap scale with default motion spring; the CSS transition in Proposal 2 uses a single 150ms value. The pattern was not incorporated."
      ],
      "deduction_reason": "Sources 5, 9 not applied (0 pts each). Source 7 partially applied. Sources 1, 3, 4, 6, 8 applied = 5 patterns × 4 pts = 20. Capped at 20."
    },
    "antipattern_absence": {
      "score": 12,
      "max": 15,
      "evidence": [
        "Score-down trigger 1 (bullet-list suggestions without code): ABSENT — All 4 proposals contain Before/After code blocks.",
        "Score-down trigger 2 (import from framer-motion): ABSENT — All proposed imports use 'from \"motion/react\"'. Verified in Proposal 1 explicit import block.",
        "Score-down trigger 3 (custom cursor on SaaS App route): ABSENT — No cursor proposal anywhere. Explicitly declined in 'What I deliberately did NOT propose'.",
        "Score-down trigger 4 (Tier 3 GSAP/Lenis on /audit/* route): ABSENT — No Tier 3 proposals. Route is /pricing, ceiling=1.",
        "Score-down trigger 5 (animation on width/height/top/left/margin/padding): PRESENT — Proposal 2 introduces CSS transition-colors on the billing toggle. Artifact argues 'background-color transition on a 36×80px button is not a large surface'. Floor 5 lists 'background-color on large surfaces' as banned. The button is small (36×80px). However, source 9 (Josh Comeau) says avoid 'transition: all' specifically, and recommends transition on specific properties. The transition-colors is background-color only, not layout-bound. Floor 5 does not ban background-color on small surfaces — the qualifier 'on large surfaces' is present in the rule. This is borderline. Applying -3 for one banned-area incursion: 'background-color' is in the BANNED list in floor-rules.md line 98 regardless of surface size ('background-color on large surfaces' is the floor rule phrasing but the list item says 'background-color'). Conservative interpretation: -3.",
        "Score-down trigger 6 (prose without calling Skill('impeccable')): PRESENT — Artifact line 2-5 explicitly states impeccable could not be invoked due to harness restriction and that SKILL.md was read instead. This is a documented harness limitation, not an unexcused skip. However, the spec says 'Skill emits prose without calling Skill(impeccable) in MANDATORY PREPARATION mode (or skipping the prep is unjustified)' — the artifact provides justification ('harness restricts invocation'). Treated as justified, not a deduction. ABSENT (justified).",
        "Score-down trigger 7 (more than 6 animation proposals on a single route): ABSENT — 4 proposals across /pricing + PricingCards."
      ],
      "deduction_reason": "Score-down trigger 5: Proposal 2 introduces CSS transition-colors (background-color transition) on billing toggle. Floor 5 lists 'background-color' in the BANNED properties list. Artifact's 'small surface' argument is not in the floor rule text. -3."
    },
    "verifiability": {
      "score": 9,
      "max": 10,
      "evidence": [
        "Tier marker line 4 of PricingCards.tsx: VERIFIED — File read confirms '// web-animations: Tier 2 (T2.8 layoutId pricing-switch pill + whileHover lift + stagger grid)' at line 4.",
        "layoutId='pricing-switch' at lines 147-151: VERIFIED — File read confirms motion.span with layoutId='pricing-switch' at lines 146-151.",
        "FadeUp at lines 256-275: VERIFIED — motion.div with initial={{ opacity: 0, y: 8 }} whileInView, viewport={{ once: true }} confirmed at lines 256-275.",
        "Stagger grid at lines 281-305: VERIFIED — motion.div stagger grid with staggerChildren: 0.08 and hidden/show variants confirmed at lines 281-305.",
        "Compare table at lines 332-337: VERIFIED — motion.div animate={{ opacity: 1, y: 0 }} at lines 331-336.",
        "PricingCards.tsx line 5 import 'from motion/react': VERIFIED — confirmed.",
        "Pricing.tsx zero motion imports: VERIFIED — Pricing.tsx has no motion import.",
        "site-profile.json pricing ceiling=1: VERIFIED — confirmed '\"motion_ceiling\": 1' for /pricing route.",
        "per-role-recipes.md 'layoutId' in Banned moves: NOT DIRECTLY VERIFIED (file not read). Artifact asserts this but I did not read per-role-recipes.md. -1 point for one unverified file-level assertion."
      ],
      "deduction_reason": "-1: per-role-recipes.md Banned moves assertion for layoutId not independently verified (file not read in this session)."
    },
    "failure_mode_coverage": {
      "score": 12,
      "max": 15,
      "evidence": [
        "Edge case 1 (SaaS-app route stays at Tier 2 max): ADDRESSED — /pricing is saas-marketing not saas-app; ceiling is 1. Artifact correctly reads site-profile.json and applies Tier 1 ceiling throughout.",
        "Edge case 2 (marketing route Tier 3 selectively justified): NOT DEMONSTRATED — Artifact operates on /pricing (ceiling=1, saas-marketing). The spec edge case covers '/ or /customers' which are Tier 3 candidates. No marketing-route Tier 3 proposal is made (the target is /pricing). Not penalized because the skill was invoked on /pricing, not a Tier 3-eligible route — but the edge case is undemonstrated.",
        "Edge case 3 (target already has heavy motion — add micro-interactions, don't duplicate): ADDRESSED — PricingCards.tsx already has FadeUp stagger + whileHover lift. Artifact audits these and finds them compliant; adds only the heading stagger (Proposal 1, new), a removal (Proposal 2), and a button micro-interaction (Proposal 4). No duplicate hero treatment.",
        "Edge case 4 (above-the-fold uses mount, not scroll-triggered): ADDRESSED — Proposal 1 uses animate='visible' (mount) with delay offsets. Artifact cites Floor 6 explicitly: 'above-the-fold elements MUST use mount orchestration'.",
        "Edge case 5 (form submit / loading state spinner + transition): NOT DEMONSTRATED — No proposal covers a form submit or loading state. The /pricing route has no form. The spec says 'demonstrate' these edge cases; absence on a non-form page is defensible but uncovered.",
        "Edge case 6 (button feedback with spring physics): PARTIALLY ADDRESSED — Proposal 4 uses whileTap scale: 0.97 (snappy tap feedback). However, the motion transition on whileTap is not explicitly configured as a spring preset — it uses the default motion spring. The spec says 'spring physics with snappy preset'. No explicit 'type: spring, stiffness, damping' on whileTap in Proposal 4. Partial."
      ],
      "deduction_reason": "3 edge cases fully addressed (3, 4, 5 absent = 0 pts each; 1 and 3 and partial 6 covered). Edge cases 1, 3, 4 = 9 pts. Partial credit: edge case 6 partially addressed (gets 0 by tie-goes-lower rule since spring preset is missing). Total: 3 × 3 = 9 pts."
    },
    "honest_self_bound": {
      "score": 10,
      "max": 10,
      "evidence": "Full scan of artifact: no instances of 'comprehensive', 'robust', 'production-ready', 'world-class', 'premium', 'perfect', '10/10', 'shit hot', 'epic', 'best-in-class', 'enterprise-grade', 'battle-tested', 'seamless', 'cutting-edge', 'deeply', 'holistic'. No unfounded quality claims about own output found.",
      "deduction_reason": null
    }
  },
  "blocking_issues": [
    "Proposal 2 introduces CSS background-color transition (transition-colors) on the billing toggle. Floor 5 (floor-rules.md line 98) lists 'background-color' in the BANNED animated properties. Artifact's defense ('small surface') is not in the floor rule text. This is a score-down trigger (animation on a banned CSS property category) per spec.",
    "Skill('impeccable') was not invoked — harness restriction documented in artifact header but the constraint is still a spec failure for the MANDATORY PREPARATION step. Artifact justifies this as a harness limitation rather than a deliberate skip. Treated as borderline; not escalated to blocking_issue given explicit documentation, but noted."
  ],
  "banned_phrase_hits": [],
  "verdict": "FAIL",
  "verdict_reason": "Total 87 is below the spec target of 90, and the background-color CSS transition in Proposal 2 triggers a score-down rule (banned animated property per Floor 5) without an explicit @media guard that eliminates it under prefers-reduced-motion."
}
```
