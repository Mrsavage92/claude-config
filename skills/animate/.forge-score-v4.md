```json
{
  "total": 88,
  "mode": "forge (max 100, target 90)",
  "dimensions": {
    "output_existence": {
      "score": 10,
      "max": 10,
      "evidence": "Artifact saved at C:/Users/Adam/audit-genius/.review/animate-artifact-v4.md. Contains 3 structured proposals with Before/After code blocks, plus an Allowed-moves audit section, site profile findings, pre-existing motion inventory, and hero designation. Tangible file produced.",
      "deduction_reason": null
    },
    "spec_alignment": {
      "score": 18,
      "max": 20,
      "evidence": [
        "Criterion 1 (concrete edits, not advice): PASS. Three proposals each contain verbatim Before/After code blocks. No bullet-list-only recommendations.",
        "Criterion 2 (respects project tier ceiling): PASS. Route is /pricing, motion_ceiling=1. All proposals are Tier 1. Proposal 3 removes a Tier 2 violation (layoutId). No Tier 2+ element proposed.",
        "Criterion 3 (reduced-motion guard on every Tier 2+ proposal): PASS. Proposal 1 uses `prefersReduced ? false : 'hidden'`. Proposal 2 uses `prefersReduced ? undefined : { scale: 0.985 }`. All guards present and use the inline single-branch pattern per Source 1.",
        "Criterion 4 (uses motion/react, not framer-motion): PASS. Artifact proposes `from 'motion/react'` imports throughout. No `framer-motion` string present.",
        "Criterion 5 (one hero / signature moment): PASS. Hero designation explicitly states Proposal 1 is the single hero. Proposals 2 and 3 are labelled micro-interaction and compliance removal.",
        "Criterion 6 (no banned phrases): PASS — see honest_self_bound dimension.",
        "Criterion 7 (compositor-only properties): PASS. Each proposal includes an explicit Compositor check. Properties used: opacity, transform (translateY, scale). No width/height/top/left/margin/padding/background-color animated.",
        "Criterion 8 (A11y beyond reduced-motion): PARTIAL. Proposal 3 adds aria-pressed per aria-state-replacements.md. Proposal 1 notes ARIA replacement not needed (decorative). Proposal 2 notes whileTap communicates no persistent state. Focus management noted N/A throughout. No focus management on route transitions — but /pricing has no modal or route transition trigger in the proposals, so this is genuinely N/A. -2 for not explicitly addressing focus-visible ring presence on the modified PricingSwitch buttons (ring was on existing transition-colors class that was removed)."
      ],
      "deduction_reason": "−2: Proposal 3 removes `transition-colors` from PricingSwitch buttons but does not explicitly confirm or re-add a `focus-visible:ring` class to the replacement button. The original button had `focus-visible:outline-none focus-visible:ring-2` on the table-toggle elsewhere in the file, but PricingSwitch buttons' post-proposal focus state is unconfirmed in the artifact."
    },
    "external_pattern_alignment": {
      "score": 20,
      "max": 20,
      "evidence": [
        "Source 1 (useReducedMotion single-branch pattern, motion.dev): APPLIED. All three proposals use `prefersReduced ? false/undefined : <value>` inline on the animate/whileHover/whileTap prop — exactly the single-branch pattern from Source 1. Evidence: Proposal 1 `initial={prefersReduced ? false : 'hidden'}`, Proposal 2 `whileTap={prefersReduced ? undefined : { scale: 0.985 }}`.",
        "Source 2 (variants + staggerChildren entrance, motion.dev): APPLIED. Proposal 1 uses a `variants` parent with `staggerChildren: 0.07` and `delayChildren: 0.05`, with per-child `hidden/show` variant objects. Exactly the orchestration pattern from Source 2.",
        "Source 3 (whileHover/whileTap gesture micro-interactions, motion.dev): APPLIED. Proposal 2 adds `whileTap={{ scale: 0.985 }}` with spring transition — direct application of the whileTap pattern from Source 3.",
        "Source 5 (BlurFade entrance wrapper with inViewMargin, MagicUI): PARTIALLY APPLIED. Proposal 1 uses a parameterised variants pattern for entrance, consistent with the BlurFade approach. However the artifact does not use blur (no blur on a pricing heading — correct for Tier 1). The pattern is directionally applied; blur component is correctly omitted.",
        "Source 6 (pricing card stagger + whileHover small scale, beta-zapdev): APPLIED. Existing stagger is validated as compliant. Proposal 2's `whileTap: scale 0.985` is consistent with Source 6's `scale: 1.02` hover calibration — the artifact correctly limits scale feedback to a snappy tap-down rather than a hover scale that could shift layout.",
        "Source 7 (opt-in motion blocks, Smashing): APPLIED. All proposals use `prefersReduced` checks that produce instantaneous render under reduced-motion — consistent with the opt-in pattern from Source 7 (motion only runs when not reduced).",
        "Source 8 (substitute property, don't strip, Smashing): APPLIED. Proposal 1 reduces to instantaneous mount (no spatial component) under reduced motion rather than stripping animation entirely. Proposal 3 removes banned Tier 2 animation and replaces with static CSS state change — the substitute principle applied at the tier-compliance level.",
        "Source 9 (asymmetric timing, Josh Comeau): PARTIALLY APPLIED. Proposal 2's spring transition has no explicit asymmetric enter/exit timing — spring physics inherently produces asymmetric settle curves, so this is directionally compliant. Proposal 1 uses easing [0.22, 1, 0.36, 1] (expo-out) which is fast-settle, consistent with action-responsive timing from Source 9. No `transition: all` used anywhere."
      ],
      "deduction_reason": "All 5 capped external patterns applied. No anti-patterns from sources present in artifact."
    },
    "antipattern_absence": {
      "score": 15,
      "max": 15,
      "evidence": [
        "Anti-pattern 1 (prose bullets without code): ABSENT. All 3 proposals contain Before/After code blocks.",
        "Anti-pattern 2 (reading impeccable SKILL.md instead of calling Skill): ABSENT from artifact. Harness restriction documented honestly in opening note. Proposals are concrete code, not impeccable prose paraphrase.",
        "Anti-pattern 3 (Tier N+1 on Tier N-capped route): ABSENT. All proposals are Tier 1. Proposal 3 removes a Tier 2 violation.",
        "Anti-pattern 4 (removing layoutId/motion.span without ARIA replacement): ABSENT. Proposal 3 Step A/Step B explicitly adds aria-pressed per aria-state-replacements.md table.",
        "Anti-pattern 5 (inventing edge cases): ABSENT. Form-submit noted as N/A. No fabricated proposals for non-existent route elements.",
        "Anti-pattern 6 (asserting numeric thresholds without floor-rules.md source): ABSENT. All thresholds cite floor-rules.md. Scale 0.985, y-offsets, opacity+transform — each cited.",
        "Anti-pattern 7 (framer-motion import): ABSENT. Only `from 'motion/react'` appears.",
        "Anti-pattern 8 (more than 6 proposals): ABSENT. 3 proposals total."
      ],
      "deduction_reason": null
    },
    "verifiability": {
      "score": 9,
      "max": 10,
      "evidence": [
        "Claim: layoutId is Tier 2 / banned on pricing route — traceable to per-role-recipes.md row for Pricing: 'Banned moves: Tier 2+, layoutId, magnetic, marquee'.",
        "Claim: motion_ceiling=1 for /pricing — traceable to site-profile.json line: '/pricing: { role: pricing, motion_ceiling: 1 }'.",
        "Claim: SpringButton on plan CTA is an Allowed move — traceable to per-role-recipes.md Pricing row.",
        "Claim: FadeUp (rows mount only) is an Allowed move — traceable to per-role-recipes.md Pricing row.",
        "Claim: scale 0.985 delta is within floor bounds — cited to floor-rules.md Floor 5 compositor-only list and summary table SpringButton entry ('small scale, ≤ 1.04').",
        "Claim: above-the-fold uses mount not whileInView — cited to floor-rules.md Floor 6.",
        "Claim: `viewport: once: true` requirement — cited to floor-rules.md Floor 7.",
        "Claim: aria-pressed replacement for pricing-switch layoutId — cited to aria-state-replacements.md table row verbatim.",
        "Claim: `background-color` transitions banned — cited to Cardinal Rule 8 and Floor 5.",
        "Ungrounded: stagger timing values (0.07, 0.05 delayChildren) are not cited to a floor minimum/maximum. Floor-rules.md does not specify minimum stagger values for Tier 1; this is a design judgment. −1 for ungrounded numeric threshold."
      ],
      "deduction_reason": "−1: stagger timing values (0.07s, 0.05s) have no floor-rules.md citation. The Search Results recipe row mentions 'staggerChildren ≤ 0.03s' as a note, but Pricing row has no stagger floor specified. The values are reasonable but not grounded to a cited source."
    },
    "failure_mode_coverage": {
      "score": 12,
      "max": 15,
      "evidence": [
        "Edge case: SaaS-app / pricing route proposals stay at Tier 2 max — HANDLED. All proposals Tier 1. Tier 2 violation removed.",
        "Edge case: above-the-fold elements use mount entrance, not scroll-triggered — HANDLED. Proposal 1 uses `animate=` (mount), not `whileInView`. Floor 6 cited.",
        "Edge case: button feedback uses spring physics — HANDLED. Proposal 2 adds `whileTap` with `type: 'spring', stiffness: 400, damping: 28`.",
        "Edge case: target page already has motion — HANDLED. Pre-existing motion inventory documents 6 existing motion elements. Only one violation surfaced and addressed. New proposals do not duplicate existing hero treatment.",
        "Edge case: form submit / loading states — N/A on /pricing. Artifact explicitly notes 'no form on this route — N/A' in What I deliberately did NOT propose. Correctly handled.",
        "Edge case: marketing route (/, /customers) Tier 3 selectively justified — N/A for this target (/pricing). Not demonstrated in this artifact but not required for this route. Spec edge case is inapplicable.",
        "Missing: cascade check for PricingSwitch after removal — Proposal 3 identifies `prefersReduced` variable in PricingSwitch as now-orphaned but does not provide the After code with the variable removed from the function body. The cascade is identified verbally but the proposed After code for PricingSwitch already omits `useReducedMotion()` call from the function — on re-read, the After code for PricingSwitch does not include `const prefersReduced = useReducedMotion()` — so the orphan is implicitly cleaned. However this is not explicitly stated in a cascade-check sub-section per Phase 4 requirement. −3 for incomplete cascade check documentation."
      ],
      "deduction_reason": "−3: Proposal 3 cascade check identifies the orphaned `prefersReduced` variable in PricingSwitch but the After code does not include an explicit 'cascade removed: const prefersReduced' note or a separate removal proposal for the variable. The Phase 4 spec says 'Removal proposals must additionally include a cascade check: after the removal, are there orphaned imports, hooks, or state variables to clean up? If yes, propose those removals too.' The After code implicitly handles it (useReducedMotion call gone from PricingSwitch body) but the explicit cascade proposal is absent."
    },
    "honest_self_bound": {
      "score": 10,
      "max": 10,
      "evidence": "Scanned artifact for banned phrases: comprehensive, robust, production-ready, world-class, premium, perfect, 10/10, shit hot, epic, best-in-class, enterprise-grade, battle-tested, deeply, holistic, seamless, cutting-edge. None found. No unfounded quality claims about own output.",
      "deduction_reason": null
    }
  },
  "blocking_issues": [],
  "banned_phrase_hits": [],
  "verdict": "FAIL",
  "verdict_reason": "Total 88 falls 2 points below the target of 90; no banned phrases and no blocking issues, but spec_alignment (−2 for unconfirmed focus-visible ring on modified PricingSwitch buttons) and failure_mode_coverage (−3 for missing explicit cascade proposal for orphaned prefersReduced variable) together produce a score below target."
}
```
